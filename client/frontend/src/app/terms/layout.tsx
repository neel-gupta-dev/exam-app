import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Terms of Service | Vayl',
};

export default function TermsofServiceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
