import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Classroom | Vayl',
};

export default function ClassroomLayout({ children }: { children: React.ReactNode }) {
  return children;
}
